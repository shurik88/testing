Целью данной недели является:
•	научиться получать необходимые данные из json объекта
•	научиться работать с результатами запроса (дата, массив, объект)
•	уметь подготавливать данные в prerequest script
•	управлять порядком выполнения запросов
•	использовать динамические переменные постмана
•	изучить основы программирования (цикл, массив, переменная)

Вам понадобится просмотреть самостоятельно информацию по javascript, есть очень хороший учебник (https://learn.javascript.ru/). Конкретно там можно посмотреть про:
•	циклы (https://learn.javascript.ru/while-for)
•	ветвления (https://learn.javascript.ru/ifelse)
•	переменные (https://learn.javascript.ru/variables)
•	массивы (https://learn.javascript.ru/array)
•	строки (https://learn.javascript.ru/string)
Важно!!! Постман не поддерживает полностью современный синтаксис js, поэтому обязательно читайте официальную документацию постмана (https://learning.postman.com/docs/writing-scripts/script-references/postman-sandbox-api-reference/ ). 
Как работать с динамическими переменными https://learning.postman.com/docs/writing-scripts/script-references/variables-list/.

Теперь по заданию
Также делается feature branching от ветки week4. Также делаем в ветку week4 c assigner меня указываете, reviewer – Ангелину.  В папке week4 вы должны создать папки со своими именами: elena, andrew и т.д. 
Далее в этих папках должны лежать два экспортированных файла: коллекция и переменные окружения (обычные). С именами «week4_{ваше имя}_{тип}.json, например  week4_andrew_col.json и week4_andrew_env.json.
В коллекции должны быть 7 запросов с выполненными заданиями в них. 

1.	GetWeather (api/weather). В этом запросе вы должны попробовать писать ветвления, обрабатывать ответы с сервера и использовать динамические переменные постмана. 
Здесь необходимо передать случайное количество записей в запросе, используя случайное число постмана (см документацию https://learning.postman.com/docs/writing-scripts/script-references/variables-list/ ). 
В случае успешного ответа (успешный ответ - это определенный статус код, см api-docs) от сервера необходимо вывести в консоль «Success {{количество элементов возвращенных в ответе}}». 
В случае неуспешного, т.е. любого другого отличного от успешного статус кода, необходимо вывести тело ответа аналогично в консоль. 
Подсказка: прежде чем слепо брать и писать «pm.response.json();» посмотрите внимательно в каком формате (см. Content-Type) сервер возвращает ответ, здесь можно решить эту проблему двумя путями: 
1 способ – заставить сервер возвращать ответ в нужном вам формате, 2 способ – найти как правильно парсить ответ в нужный формат. Попробуйте оба варианта обязательно!

2.	GetTodos(api/todos). В этом запросе вы должны получить ответ от сервера, и если в ответе присутствует хотя бы один элемент, дедлайн которого попадает в интервал +-7 дней от текущей даты, то вы должны взять этот элемент из коллекции (или просто первый элемент) 
записать все его редактируемые поля и его идентификатор в переменные окружения и сделать следующим запрос на исполнение EditTodo (put api/todos/{{id}}). 
Иначе ничего не делать (запросы пойдут в порядке, указанном в задании). Чтобы проверить этот сценарий (когда элементы точно отсутствуют) вам понадобится либо удалить все данные через интерфейс, либо, что предпочтительнее, остановить все контейнеры в docker desktop и удалить контейнер mongo, а потом запустить все заново.
Как работать с датой посмотрите здесь https://momentjs.com/ как подключить момент смотрите лекцию Ангелины или здесь https://learning.postman.com/docs/writing-scripts/script-references/postman-sandbox-api-reference/ . 
Важно: Не забывайте, что поле дедлайна не является обязательным, подумайте как вы будете обрабатывать пустые значения, см документация по js, как if работают с пустыми значениями

3.	AddToDo (Post api/todos). В этом методе все просто в PreRequest Scripts вы должны инициализировать значения всех передаваемых полей, записав их в переменные. 
Соответственно в Tests вы должны, используя снипеты постмана написать утверждение, проверяющее успешный статус код (какой будет код смотрите api/docs). 
Далее вы должны получить возвращенный идентификатор нового созданного дела и записать его в переменную.

4.	EditToDo (Put api/todos). Если вы внимательно читали, то уже могли догадаться, что переменные окружения, используемые в задании 2(в случае наличие элементов) и задания 3 должны иметь одинаковые имена. 
В этом задании вам надо будет добавить к переменной	дедлайна 3 дня (если значение отсутствует, то к текущей дате добавить 3 дня), в теги добавить один элемент (см как работать с массивами и строками в js, вам здесь понадобится переменная $randomAbbreviation), приоритет (случайное число, удовлетворяющее диапазону, указанному в api-docs) 
здесь надо написать код на js, как? Предлагаю вам погуглить, есть много вариантов. 
В поле Text – случайная, уникальная строка, используйте в качестве суффикса guid. В поле AssignedTo записать пару случайное имя, случайный email, см динамические переменные постмана. Идентификатор элемента вы берете из записанной ранее переменной. В тестах соответственно вы должны проверить, что запрос прошел успешно.

5.	GetToDo (Get api/todos/{{id}}). Здесь делайте запрос по идентификатору дела, записанной в соответствующей переменной. 
В тестах вы должны проверить, что запрос успешный, а также проверить, что все переданные в запросе 4 параметры правильно применились, для этого можете использовать снипет постмана «Response body: Json value check». 

6.	 DeleteToDo (Delete api/todos/{{id}}). Здесь необходимо подчистить за собой. Т.е вы должны удалить дело и проверить успешный статус код.

7.	 GetDeletedToDo (Get api/todos/{{id}}). Здесь вы должны проверить, что дело отсутствует, как смотрите api/docs

Если хотите самостоятельно проверить как работает запуск всех запросов в коллекции, можете почитать как использовать collection runner (https://learning.postman.com/docs/running-collections/intro-to-collection-runs/) 
или newman  (https://learning.postman.com/docs/running-collections/using-newman-cli/command-line-integration-with-newman/) а лучше попробуйте оба способа 😊 
